<?php 

// PHP Constants
// A constant is an identifier (name) for a simple value. The value cannot be changed or undefined.
// A valid constant name starts with a letter or underscore (no $ sign before the constant name).
// NoteL Unlike variables, constants are auyomatically global across the entire script.

# define( name, value, case-insensitive );

define( "domainName", "https://frontenddesign.ir/" );
echo domainName;